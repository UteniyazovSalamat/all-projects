import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <img className="header__img" src="./images/picture/bg-header-desktop.svg" alt="bg-header" />
        </header>
    );
};

export default Header;
